import 'package:flutter/material.dart';

final containerDecoration = BoxDecoration(
  borderRadius: BorderRadius.circular(8.0),
  color: Colors.grey[200],
);

const mobileScreenBox = BoxConstraints(maxWidth: 550);
